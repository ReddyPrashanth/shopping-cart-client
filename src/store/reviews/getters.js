export default {
    reviews(state) {
        return state.reviews;
    },
    next(state) {
        return state.next;
    },
    prev(state) {
        return state.prev;
    },
    currentPage(state) {
        return state.current_page;
    }
}